#define _BUF_SIZE 100
#define YY_BUF_SIZE ( _BUF_SIZE * 2 )
#define YY_READ_BUF_SIZE ( _BUF_SIZE )

fprintf(stdout, "buf: %s\n", yytext_ptr);
fprintf(stdout, "number_to_move: %d\n", number_to_move);

